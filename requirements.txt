numpy>=1.24,<3
pandas>=2.0
openpyxl>=3.1
matplotlib>=3.7
scikit-learn>=1.3
Pillow
# The XGBoost companion's holdout metrics, gain importances and persistence
# projection reported in the article were generated with XGBoost 2.1 and are
# reproduced exactly by 2.0.3 and 2.1.4. XGBoost 3.x changes tree-construction
# defaults and gives different values, so the 2.x series is pinned.
xgboost>=2.0,<3
